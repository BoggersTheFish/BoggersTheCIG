# Minimal test runner dependencies.
#
# The full runtime stack is listed in requirements.txt. Use this file when you
# only need the pytest runner available before installing the heavier project
# dependencies.
pytest>=7.4.0
pytest-asyncio>=0.23.0
networkx>=3.2.0
