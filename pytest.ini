[pytest]
markers =
    integration: tests that exercise broader runtime workflows or mutate integration surfaces
    slow: tests that perform plotting, snapshots, or other slower checks
    external: tests that require network, local services, model downloads, or live external processes
